[project]
name = "pdf-handler"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "pypdf>=6.9.1",
    "typer>=0.24.1",
]

[project.scripts]
pdf = "pdf_handler:app"
